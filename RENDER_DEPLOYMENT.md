# نظام إرسال دعوات واتساب التفاعلي 📨

نظام متكامل لإرسال دعوات حفلات التخرج والفعاليات عبر WhatsApp Business API مع أزرار تفاعلية ورد تلقائي.

## المميزات الرئيسية ✨

- ✅ **إرسال دعوات تفاعلية** مع أزرار (تأكيد / اعتذار)
- ✅ **رد تلقائي** بعد اختيار المدعو
- ✅ **لوحة تحكم** لمتابعة الردود في الوقت الفعلي
- ✅ **إحصائيات مباشرة** مع رسوم بيانية
- ✅ **تصدير النتائج** إلى CSV
- ✅ **إرسال جماعي** لآلاف الدعوات
- ✅ **حساب Business معتمد** - إرسال لأي رقم مباشرة

## التشغيل على Render.com (مجاني) 🚀

### 1. إنشاء حساب على Render
1. اذهب إلى [render.com](https://render.com)
2. سجّل حساب جديد أو سجّل الدخول

### 2. ربط المشروع من GitHub
1. في Render Dashboard، اضغط **New +** ← **Web Service**
2. اختر **Connect GitHub** وصرّح لـ Render بالوصول
3. اختر repository: `harbib-989/whatsapp-invitation-system`
4. اضغط **Connect**

### 3. إعداد Web Service

في صفحة إعداد الخدمة:

#### الإعدادات الأساسية:
- **Name**: `whatsapp-invitation-system` (أو اسم تختاره)
- **Region**: Oregon (US West) - الأقرب للمملكة
- **Branch**: `main`
- **Root Directory**: (اتركه فارغاً)
- **Runtime**: `Python 3`
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `gunicorn webhook_server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 2`

#### Plan:
- اختر **Free** (مجاني)

### 4. إضافة Environment Variables (متغيرات البيئة)

اضغط **Advanced** ← **Add Environment Variable** وأضف:

#### متغيرات Twilio (مطلوبة):
```
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM_PHONE=whatsapp:+966XXXXXXXXX
```

#### متغيرات القوالب (اختيارية):
```
EVENT_MODE=dialogue
CONTENT_SID=HX3f0e4a3d084e11732b364230057c64aa
CONTENT_SID_VIP=HXe8c0b79c33f7c9c1254f74c39ba547fb
```

### 5. إنشاء الخدمة
اضغط **Create Web Service**

Render سيقوم بـ:
- ✅ بناء المشروع (Build)
- ✅ تثبيت المكتبات
- ✅ تشغيل السيرفر
- ✅ إنشاء رابط عام مثل: `https://whatsapp-invitation-system.onrender.com`

### 6. اختبار السيرفر

بعد اكتمال النشر:
1. افتح الرابط الذي أنشأه Render
2. يجب أن ترى لوحة التحكم (Dashboard)
3. اختبر بإرسال دعوة لرقمك الشخصي

## الوصول إلى النظام 🌐

### الروابط الرئيسية:

- **لوحة التحكم**: `https://your-app-name.onrender.com/dashboard`
- **صفحة الدعوة**: `https://your-app-name.onrender.com/invitation`
- **Health Check**: `https://your-app-name.onrender.com/health`
- **Webhook URL**: `https://your-app-name.onrender.com/webhook`

## إعداد Twilio Webhook 📞

لاستقبال ردود المدعوين:

1. اذهب إلى [Twilio Console](https://console.twilio.com)
2. WhatsApp → Senders → اختر رقمك
3. في **Webhook Configuration**:
   - **When a message comes in**: `https://your-app-name.onrender.com/webhook`
   - **Method**: `POST`
4. احفظ التغييرات

## إرسال الدعوات 📨

### من لوحة التحكم:

1. افتح: `https://your-app-name.onrender.com/dashboard`

2. **إرسال دعوة واحدة:**
   - اكتب الاسم
   - اكتب رقم الجوال (مثل: 0501234567)
   - اختر نوع الدعوة
   - اضغط "إرسال الدعوة"

3. **إرسال جماعي:**
   - انسخ قائمة المدعوين (اسم، رقم)
   - الصق في مربع "إرسال جماعي"
   - اضغط "إرسال جماعي"

### الصيغة للإرسال الجماعي:

```
أحمد محمد، 0551234567
سارة علي، 0559876543
عبدالله سعد، 0501112233
```

## مراقبة الردود 📊

في لوحة التحكم سترى:
- ✅ إجمالي المدعوين
- ✅ عدد الردود
- ✅ تأكيدات الحضور
- ✅ الاعتذارات
- ✅ بانتظار الرد

يمكنك تصدير النتائج إلى CSV من زر "تصدير CSV"

## ملاحظات هامة ⚠️

### الخطة المجانية على Render:
- ✅ مجانية تماماً
- ⏰ السيرفر ينام بعد 15 دقيقة من عدم النشاط
- ⏱️ يستيقظ تلقائياً عند أول طلب (قد يستغرق 30-60 ثانية)
- 💾 لا يوجد تخزين دائم (تُحذف البيانات عند إعادة التشغيل)

### للاستخدام الفعلي:
- احفظ نسخة من `invitees.json` و `responses.json` محلياً
- أو ترقية إلى Render Paid Plan للتخزين الدائم

### التكاليف (Twilio):
- 📨 كل دعوة: ~$0.005 - $0.01
- 📩 كل رد تلقائي: ~$0.005 - $0.01
- 💰 المجموع: ~$0.01 - $0.02 لكل مدعو

## حل المشاكل الشائعة 🔧

### السيرفر لا يستجيب:
- انتظر 30-60 ثانية (السيرفر يستيقظ من السكون)
- تحقق من Render Logs في Dashboard

### الدعوات لا تصل:
- تأكد من صحة `TWILIO_ACCOUNT_SID` و `TWILIO_AUTH_TOKEN`
- تأكد من صحة `TWILIO_FROM_PHONE`
- تحقق من Twilio Console → Logs

### الردود لا تُحفظ:
- تأكد من Webhook URL في Twilio
- تحقق من Render Logs

## التشغيل المحلي (للتطوير) 💻

```bash
# 1. استنساخ المشروع
git clone https://github.com/harbib-989/whatsapp-invitation-system.git
cd whatsapp-invitation-system

# 2. إنشاء بيئة افتراضية
python -m venv venv
source venv/bin/activate  # في Windows: venv\Scripts\activate

# 3. تثبيت المكتبات
pip install -r requirements.txt

# 4. إضافة ملف .env
# أنشئ ملف .env وأضف:
# TWILIO_ACCOUNT_SID=ACxxxxxxxx
# TWILIO_AUTH_TOKEN=xxxxxxxx
# TWILIO_FROM_PHONE=whatsapp:+966550308539

# 5. تشغيل السيرفر
python webhook_server.py

# 6. فتح المتصفح
# http://localhost:5000/dashboard
```

## البنية الأساسية للمشروع 📁

```
whatsapp-invitation-system/
├── webhook_server.py           # السيرفر الرئيسي (Flask + Gunicorn)
├── whatsapp_sender.py         # أداة الإرسال المحلية
├── dashboard.html             # لوحة التحكم
├── whatsapp_invitation.html   # صفحة الدعوة
├── attendance_form.html       # نموذج تأكيد الحضور
├── decline_form.html          # نموذج الاعتذار
├── requirements.txt           # المكتبات المطلوبة
├── Procfile                   # أمر التشغيل لـ Render
├── render.yaml                # إعدادات Render
├── config.json               # إعدادات الحساب والقوالب
├── invitees.json             # قاعدة بيانات المدعوين
└── responses.json            # قاعدة بيانات الردود
```

## القوالب المعتمدة ✅

النظام يدعم 3 قوالب معتمدة من WhatsApp:

1. **دعوة عامة** - للحضور العام
2. **دعوة رسمية** - للمسؤولين (تتطلب المنصب)
3. **ملتقى الكفاءات** - لفعاليات التوظيف

راجع `APPROVED_TEMPLATES.md` للتفاصيل.

## الدعم والمساعدة 💬

- 📖 راجع ملفات MD في المشروع
- 🐛 بلّغ عن مشاكل: GitHub Issues
- 📧 تواصل مع مطور المشروع

## الترخيص 📄

هذا المشروع مفتوح المصدر - استخدمه بحرية!

---

**آخر تحديث**: فبراير 2026

**الحالة**: ✅ جاهز للإنتاج على Render
