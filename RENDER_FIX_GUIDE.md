# دليل حل مشكلة Render - الموقع ما يفتح 🔧

## المشكلة الأساسية 🚨

عند فتح الموقع على Render، قد تواجه:
1. ⏰ **السيرفر نائم** - Render المجاني ينام بعد 15 دقيقة
2. 🔴 **صفحة فارغة أو خطأ 404**
3. ❌ **الدعوات لا تصل**

---

## الحل السريع (خطوة بخطوة) ✅

### الخطوة 1: تحديث الكود على GitHub

قمت بإصلاح المشاكل في الكود. الآن قم برفع التحديثات:

```bash
cd c:\Users\b_asi\send_whats

# إضافة جميع الملفات المعدلة
git add .

# عمل commit
git commit -m "Fix Render deployment: simplify paths, add health check, add render.yaml"

# رفع على GitHub
git push origin main
```

### الخطوة 2: إعادة نشر المشروع على Render

1. اذهب إلى [Render Dashboard](https://dashboard.render.com)
2. اختر مشروعك `whatsapp-invitation-system`
3. في أعلى الصفحة، اضغط **Manual Deploy** ← **Deploy latest commit**
4. انتظر حتى يكتمل البناء (Build) والنشر (Deploy)

### الخطوة 3: اختبار السيرفر

بعد اكتمال النشر:

1. افتح رابط Render (مثل: `https://whatsapp-invitation-system.onrender.com`)
2. **مهم**: انتظر 30-60 ثانية في المرة الأولى (السيرفر يستيقظ)
3. يجب أن ترى لوحة التحكم

#### اختبار Health Check:
افتح: `https://your-app-name.onrender.com/health`

يجب أن ترى:
```json
{
  "status": "ok",
  "message": "WhatsApp Invitation System is running",
  "timestamp": "2026-02-14T...",
  "version": "2.0"
}
```

---

## إعداد متغيرات البيئة على Render 🔑

تأكد من إضافة هذه المتغيرات في Render Dashboard:

### كيفية إضافة المتغيرات:

1. في Render Dashboard → اختر مشروعك
2. اذهب إلى **Environment**
3. اضغط **Add Environment Variable**

### المتغيرات المطلوبة:

```
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_FROM_PHONE=whatsapp:+966XXXXXXXXX
```

### المتغيرات الاختيارية (للقوالب):

```
EVENT_MODE=dialogue
CONTENT_SID=HX3f0e4a3d084e11732b364230057c64aa
CONTENT_SID_VIP=HXe8c0b79c33f7c9c1254f74c39ba547fb
```

**بعد إضافة المتغيرات**: اضغط **Save Changes** - سيعيد Render نشر المشروع تلقائياً.

---

## إعداد Webhook في Twilio 📞

لاستقبال الردود من المدعوين:

### الخطوات:

1. اذهب إلى [Twilio Console](https://console.twilio.com/us1/develop/sms/senders/whatsapp-senders)
2. اختر رقمك: **+966550308539**
3. ابحث عن **Webhook Configuration** أو **When a message comes in**
4. ضع:
   ```
   https://your-app-name.onrender.com/webhook
   ```
   (استبدل `your-app-name` باسم مشروعك الفعلي على Render)
5. اختر **HTTP POST**
6. احفظ التغييرات

---

## اختبار كامل للنظام 🧪

### 1. اختبار الموقع:

افتح في المتصفح:
```
https://your-app-name.onrender.com/dashboard
```

يجب أن ترى:
- ✅ لوحة التحكم
- ✅ إحصائيات (قد تكون صفر في البداية)
- ✅ نموذج إرسال الدعوات

### 2. اختبار إرسال دعوة:

1. في لوحة التحكم، املأ:
   - **الاسم**: اسمك
   - **رقم الجوال**: رقمك (مثل: 0501234567)
   - **نوع الدعوة**: دعوة عامة
2. اضغط **إرسال الدعوة**
3. انتظر رسالة التأكيد

### 3. استلام الدعوة على واتساب:

يجب أن تصلك رسالة من **الكلية التقنية بالأحساء** مع:
- 📨 نص الدعوة
- 🟢 زر "تأكيد الحضور"
- 🔴 زر "اعتذار"

### 4. اختبار الرد التلقائي:

1. اضغط أحد الأزرار في واتساب
2. يجب أن يصلك رد تلقائي فوراً
3. في لوحة التحكم، ستظهر الإحصائيات محدثة

---

## حل المشاكل الشائعة 🔧

### مشكلة 1: الموقع بطيء أو لا يفتح

**السبب**: السيرفر نائم (الخطة المجانية)

**الحل**:
- انتظر 30-60 ثانية عند أول زيارة
- السيرفر سيستيقظ تلقائياً
- للحفاظ عليه مستيقظاً: استخدم خدمة Ping مثل [UptimeRobot](https://uptimerobot.com)

### مشكلة 2: صفحة 404 - Not Found

**السبب**: مشكلة في المسارات

**الحل**:
1. تأكد من رفع آخر التحديثات على GitHub
2. أعد نشر المشروع من Render Dashboard
3. تحقق من Render Logs

### مشكلة 3: الدعوات لا تصل

**السبب**: خطأ في إعدادات Twilio

**الحل**:
1. تحقق من `TWILIO_ACCOUNT_SID` و `TWILIO_AUTH_TOKEN` في Render Environment
2. تأكد من `TWILIO_FROM_PHONE` صحيح: `whatsapp:+966550308539`
3. افتح [Twilio Logs](https://console.twilio.com/us1/monitor/logs/sms) للتفاصيل

### مشكلة 4: الردود لا تُسجل في لوحة التحكم

**السبب**: Webhook غير مفعّل في Twilio

**الحل**:
1. اذهب إلى Twilio WhatsApp Senders
2. تأكد من Webhook URL: `https://your-app-name.onrender.com/webhook`
3. تأكد من اختيار **HTTP POST**

### مشكلة 5: خطأ "Internal Server Error" (500)

**السبب**: خطأ في الكود أو المتغيرات

**الحل**:
1. افتح Render Dashboard → مشروعك → **Logs**
2. ابحث عن الأخطاء الحمراء
3. تحقق من أن جميع متغيرات البيئة موجودة

---

## فحص Render Logs 📋

لمعرفة ما يحدث في السيرفر:

1. افتح Render Dashboard
2. اختر مشروعك
3. اضغط **Logs** في القائمة اليسرى
4. شاهد:
   - ✅ Build Logs: عملية البناء
   - ✅ Deploy Logs: عملية النشر
   - ✅ Service Logs: نشاط السيرفر الحالي

---

## الترقية من الخطة المجانية (اختياري) 💰

إذا كنت تستخدم النظام بشكل مكثف:

### خطة Render Paid (~$7/شهر):
- ✅ لا ينام السيرفر
- ✅ أسرع في الاستجابة
- ✅ تخزين دائم للبيانات
- ✅ دعم فني أفضل

للترقية:
1. Render Dashboard → مشروعك
2. **Settings** → **Upgrade Plan**
3. اختر **Starter** ($7/شهر)

---

## نصائح للاستخدام الأمثل 💡

### 1. للحفاظ على السيرفر مستيقظاً (مجاناً):
استخدم [UptimeRobot](https://uptimerobot.com):
- أنشئ حساب مجاني
- أضف مراقبة (Monitor) لـ: `https://your-app-name.onrender.com/health`
- اضبط الفحص كل 5 دقائق
- سيبقى السيرفر نشطاً!

### 2. حفظ البيانات (مهم):
الخطة المجانية **لا تحفظ** `invitees.json` و `responses.json` بشكل دائم.

**الحل**:
- صدّر CSV من لوحة التحكم بانتظام
- أو استخدم خطة Paid للتخزين الدائم

### 3. السرعة:
- أول طلب قد يستغرق 30-60 ثانية
- بعدها يكون سريع
- للسرعة الفورية: ترقية إلى Paid Plan

---

## خلاصة سريعة ⚡

```bash
# 1. حفظ التغييرات على GitHub
git add .
git commit -m "Fix Render issues"
git push origin main

# 2. في Render Dashboard:
- إعادة النشر (Redeploy)
- التأكد من Environment Variables
- فحص Logs

# 3. في Twilio:
- ضبط Webhook URL
- اختبار إرسال دعوة

# 4. الاختبار:
- افتح: https://your-app.onrender.com/health
- افتح: https://your-app.onrender.com/dashboard
- أرسل دعوة تجريبية
```

---

## جاهز للاستخدام! 🎉

بعد اتباع الخطوات أعلاه:
- ✅ الموقع يعمل على Render
- ✅ لوحة التحكم تفتح بدون مشاكل
- ✅ الدعوات تُرسل وتصل
- ✅ الردود تُسجل في الوقت الفعلي

**استمتع باستخدام النظام!** 🚀
